# Variáveis de ambiente do servidor (configurar no painel da Vercel:
# Project Settings → Environment Variables — NUNCA commitar um .env real
# com a chave de verdade).

# Chave da OpenRouteService (openrouteservice.org → Dashboard → Tokens).
# Usada só pelo servidor, em api/ors-proxy.js — nunca fica exposta no
# navegador. IMPORTANTE: gere uma chave NOVA — a antiga já esteve escrita
# direto no código-fonte publicado, então deve ser considerada comprometida.
ORS_API_KEY=
